# Do not place secrets in Flutter assets or commit them to Git.
#
# Provider credentials (payment, AI, e-mail and face recognition) belong only
# in Firebase Functions / backend environment variables. This file deliberately
# contains no usable configuration.
