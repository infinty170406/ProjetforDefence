// Dummy service worker for Firebase Cloud Messaging
// Prevents the "unsupported MIME type ('text/html')" error on Web startup.
// In a real production app, you would include FCM initialization here.
